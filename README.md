# rn-playsomethingwebothlike
A React Native app that consumes the Spotify API.

The user provides urls to 2 Spotify playlists.

The app then returns a list of tracks that both playlist owners might like.
